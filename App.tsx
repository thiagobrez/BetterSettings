import type React from "react";
import { SafeAreaView, StyleSheet, Text } from "react-native";

function App(): React.JSX.Element {
	return (
		<SafeAreaView>
			<Text>BetterSettings</Text>
		</SafeAreaView>
	);
}

const styles = StyleSheet.create({});

export default App;
